// Drizzle schema — the single source of truth for the database.
//
// Table definitions (teams, people, constraints, weeks, assignments, settings)
// land in Task 2. This placeholder exists so the DB tooling (`db:generate`,
// `db:studio`, etc.) resolves a valid schema path from Task 1 onward.
export {}
